{
	"scripts": {
		"build": "lerna run build",
		"dev": "lerna run --parallel dev",
		"test": "lerna run --parallel test",
		"publish": "lerna publish"
	},
	"devDependencies": {
		"lerna": "^2.11.0",
		"typescript": "^3.6.3"
	}
}
